function mapTransCallback(msg)
%load('globalmap.mat');
local_map = rosReadOccupancyGrid(msg);


    
%show(fixmap);
%title('Global Map')

show(local_map)
title('Real Time map')
hold on


%syncWith(globalmap, local_map);
%show(globalmap)
end
